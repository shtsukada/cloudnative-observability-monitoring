{{- if and .Values.networkPolicy.enabled .Values.networkPolicy.prometheus.allowSelfScrape }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: monitoring-np-prometheus-self-ingress
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    {{- toYaml .Values.networkPolicy.selectors.prometheus | nindent 4 }}
  policyTypes: [Ingress]
  ingress:
    - from:
        - podSelector:
            {{- toYaml .Values.networkPolicy.selectors.prometheus | nindent 12 }}
      ports:
        - protocol: TCP
          port: {{ .Values.networkPolicy.ports.prometheusHttp }}
{{- end }}
